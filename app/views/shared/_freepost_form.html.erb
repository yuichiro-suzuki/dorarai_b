<%= form_for(@freepost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "ここに番号や条件を入力" %>
  </div>
  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>
