<%= form_for(@firstpost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "ここに入力" %>
  </div>
  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>
